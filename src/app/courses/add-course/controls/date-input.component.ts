import { Component, Input } from '@angular/core';

@Component({
  selector: 'date-input',
  template: `
    <input tape="text">
  `
})
class DateInputComponent /*implements ControlValueAccessor*/ {


}